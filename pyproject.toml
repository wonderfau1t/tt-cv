[project]
name = "cvmodule"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ultralytics>=8.3.240",
    "wget>=3.2",
]
